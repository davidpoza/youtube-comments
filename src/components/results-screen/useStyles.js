import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  commentsBlock: {
    marginTop: '1em',
  },
  commentCounter: {
    marginTop: '1em',
    marginBottom: '1.5em',
  },
}));
